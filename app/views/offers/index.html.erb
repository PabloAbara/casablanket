<div class="text-black mb-3 mb-md-0 p-2 bd-highlight">
  <h1>MERCADO</h1>
</div>


<table class="table">
  <thead>
    <tr class="table-info">
      <th scope="col">Usuario</th>
      <th scope="col">Día / Fecha</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
      <% @offers.each do |offer| %>
      <tr>
        <td><%= offer.assign_id %></td>
        <td><%= offer.message %></td>
        <td><%= offer.status %></td>
        <td scope="row"><button type="button" class="btn btn-warning"><%= link_to 'ME INTERESA!', new_demand_path(offer_id: offer.id) %></button></td>
      </tr>
    <% end %>
  </tbody>
</table>